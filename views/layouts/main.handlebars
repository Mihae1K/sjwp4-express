<!DOCTYPE html>
<html>

<head>
    <title>Moja Express aplikacija</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    {{#if user}}
        <div class="p-2 flex flex-row justify-end gap-3 bg-base-200 items-baseline">
            <div>{{user.name}}</div>
            <a href="/users/signout" class="btn btn-accent btn-sm">Odjava</a>
        </div>
    {{/if}}

    {{{body}}}
</body>

</html>