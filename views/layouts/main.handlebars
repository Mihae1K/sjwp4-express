<!DOCTYPE html>
<html>

<head>
    <title>Moja Express aplikacija</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    {{#if user}}
        <div class="flex flex-wrap gap-4 p-2 bg-base-200">

            <div class="flex-1 flex flex-wrap gap-3">
                <a href="/competitions" class="btn btn-accent btn-sm">Natjecanja</a>
            </div>

            <div class="flex gap-3 items-baseline">
                <a href="/users/data" class="btn btn-secondary btn-sm">{{user.name}}</a>
                <a href="/users/signout" class="btn btn-accent btn-sm">Odjava</a>
            </div>
        </div>

    {{/if}}

    {{{body}}}
</body>

</html>